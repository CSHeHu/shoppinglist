<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <header id="header">
      <h1><%= title %></h1>
    </header>
    <div id="errorMessage" style="display: none"></div>

    <div id="SLContainer">
      <!-- Items will be loaded client-side from the API -->
    </div>

    <div id="addToListContainer">
      <form id="addToListForm">
        <input
          type="text"
          id="addToListInput"
          placeholder="Add new item"
          name="name"
          required
        />
        <input
          type="number"
          id="addAmountToListInput"
          placeholder="Add amount"
          name="amount"
          required
        />
        <button type="submit" id="addToListSubmit">Add</button>
        <button type="reset" id="addToListReset">Reset</button>
      </form>
    </div>

    <script type="module" src="/mainpage.js"></script>
  </body>
</html>
